# vapor-sl-de.html 图片替换清单

所有需要你替换的图片位置，按文件顺序列出；替换后保存即可。

---

## 1. 顶部导航 Logo

| 位置 | 行号 | 当前地址 | 应替换为 |
|------|------|----------|----------|
| 导航栏左侧 Logo | 68–72 | `https://cdn.shopify.com/s/files/1/0999/0694/5363/files/Group_3.png?v=1768738492` | **Cosmosebike / COSMOSWORKS 品牌 Logo**（白底或透明 PNG，会通过 `invert` 显示为白） |

---

## 2. 主图与缩略图（产品主视觉）

主图和下方 4 张缩略图都来自同一个数组 `images`（约 421–425 行），改这里即可同时更新主图和缩略图。

| 位置 | 行号 | 当前地址 | 应替换为 |
|------|------|----------|----------|
| 主图 + 缩略图 1 | 422 | `vapor-sl-1.jpg` | **VAPOR SL 产品图 1**（建议：正面或 3/4 视角，主推配色如 Moonlight） |
| 主图 + 缩略图 2 | 423 | `vapor-sl-2.jpg` | **VAPOR SL 产品图 2**（建议：侧面或细节） |
| 主图 + 缩略图 3 | 424 | `vapor-sl-3.jpg` | **VAPOR SL 产品图 3**（建议：另一角度或骑行场景） |
| 主图 + 缩略图 4 | 425 | `vapor-sl-4.jpg` | **VAPOR SL 产品图 4**（建议：细节如电机/碳架/轮组） |

**说明**：这 4 张会轮播显示在主图区，并作为下方 4 个小图点击切换。可换成你现有的 VAPOR SL 实拍或效果图，保持横版或 4:3 比例更统一。

---

## 3. 其他（暂无图片占位）

- 页内**地图**（约 361 行）目前是注释掉的 iframe，若要用真实 Deruiz 地址，需取消注释并填入 Google 地图 embed 链接。
- 未发现其他 `img` 或 `url(...)` 图片占位。

---

**替换步骤建议**  
1. 把要用的 Logo 和 4 张产品图准备好（路径或 URL）。  
2. 在 `vapor-sl-de.html` 里：  
   - 第 69 行：把 `src="..."` 里的地址换成你的 Logo 地址。  
   - 第 421–425 行：把 `images` 数组里 4 个字符串换成 4 张产品图地址。  
3. 存盘后刷新页面检查主图与缩略图是否都更新。
